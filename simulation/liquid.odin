package simulation

import "core:fmt"
import "core:math"
// liquid_move_down :: proc(world: ^World, x, y: int) -> bool {
    
// }
// liquid_move_side :: proc(world: ^World, x, y: int) -> bool {

// }
